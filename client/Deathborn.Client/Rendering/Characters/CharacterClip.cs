namespace Deathborn.Client.Rendering.Characters;

/// <summary>Shared animation clips — every layer sheet for a clip uses the same <see cref="AnimationSpecification"/>.</summary>
public enum CharacterClip
{
    Idle,
    Walk,
    Run,
    Attack,
    Cast,
    Hurt,
    Death,
    Roll,
}
