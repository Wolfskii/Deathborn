name: Release

# Called by develop-release / manual-release, or triggered by a local `git push origin vX.Y.Z`.
on:
  workflow_call:
    inputs:
      tag:
        description: Release tag (e.g. v0.1.0)
        required: true
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

env:
  RELEASE_TAG: ${{ inputs.tag || github.ref_name }}

jobs:
  client:
    name: Client (${{ matrix.rid }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-latest
            rid: win-x64
          - runner: ubuntu-latest
            rid: linux-x64
          - runner: macos-latest
            rid: osx-x64
          - runner: macos-latest
            rid: osx-arm64
    defaults:
      run:
        working-directory: client
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.0.x"

      - name: Publish Deathborn client
        shell: bash
        env:
          VERSION: ${{ env.RELEASE_TAG }}
        run: |
          set -euo pipefail
          version="${VERSION#v}"
          dotnet tool restore
          dotnet restore Deathborn.Client.sln
          dotnet publish Deathborn.Client/Deathborn.Client.csproj \
            -c Release \
            -r "${{ matrix.rid }}" \
            --self-contained true \
            -p:Version="$version" \
            -o publish

      - name: Pack (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        working-directory: client
        env:
          RELEASE_TAG: ${{ env.RELEASE_TAG }}
        run: |
          $version = $env:RELEASE_TAG.Substring(1)
          New-Item -ItemType Directory -Force -Path ../dist | Out-Null
          Compress-Archive -Path "publish/*" -DestinationPath "../dist/Deathborn-$version-${{ matrix.rid }}.zip"

      - name: Pack (Unix)
        if: runner.os != 'Windows'
        shell: bash
        env:
          RELEASE_TAG: ${{ env.RELEASE_TAG }}
        run: |
          set -euo pipefail
          version="${RELEASE_TAG#v}"
          mkdir -p dist
          (cd client/publish && zip -r "../../dist/Deathborn-${version}-${{ matrix.rid }}.zip" .)

      - uses: actions/upload-artifact@v4
        with:
          name: client-${{ matrix.rid }}
          path: dist/*.zip
          if-no-files-found: error

  docker:
    name: Server Docker (GHCR)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=${{ env.RELEASE_TAG }}
            type=raw,value=latest

      - name: Build and push server image
        uses: docker/build-push-action@v7
        with:
          context: server
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ env.RELEASE_TAG }}

  github-release:
    name: GitHub Release (client downloads)
    needs: [client]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Flatten artifact layout
        run: |
          shopt -s nullglob
          mkdir -p release
          find dist -name '*.zip' -exec cp {} release/ \;
          ls -la release/
          test "$(ls -1 release/*.zip 2>/dev/null | wc -l)" -gt 0

      - name: Generate checksums
        run: |
          cd release
          sha256sum *.zip > SHA256SUMS.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ github.sha }}
          name: Deathborn ${{ env.RELEASE_TAG }}
          generate_release_notes: true
          files: |
            release/*.zip
            release/SHA256SUMS.txt
